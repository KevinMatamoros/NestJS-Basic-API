import { CreateMessagesDto } from './create-messages.dto.entity';

describe('CreateMessagesDto', () => {
  it('should be defined', () => {
    expect(new CreateMessagesDto()).toBeDefined();
  });
});
